
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permite acesso total para qualquer usuário autenticado (DEV MODE)
    // TODO: Reverter para regras restritivas em produção
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
