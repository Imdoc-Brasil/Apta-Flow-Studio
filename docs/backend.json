{
  "entities": {
    "Client": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Client",
      "type": "object",
      "description": "Represents a client company.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Client entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the client company."
        },
        "cnpj": {
          "type": "string",
          "description": "The CNPJ (Brazilian company identifier) of the client."
        },
        "contactEmail": {
          "type": "string",
          "description": "The primary contact email for the client.",
          "format": "email"
        },
        "phoneNumber": {
          "type": "string",
          "description": "The primary phone number for the client."
        },
        "address": {
          "type": "string",
          "description": "The physical address of the client."
        }
      },
      "required": [
        "id",
        "name",
        "cnpj"
      ]
    },
    "Employee": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Employee",
      "type": "object",
      "description": "Represents an employee within the system.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Employee entity."
        },
        "firstName": {
          "type": "string",
          "description": "The first name of the employee."
        },
        "lastName": {
          "type": "string",
          "description": "The last name of the employee."
        },
        "email": {
          "type": "string",
          "description": "The email address of the employee.",
          "format": "email"
        },
        "jobTitle": {
          "type": "string",
          "description": "The job title of the employee."
        },
        "department": {
          "type": "string",
          "description": "The department the employee belongs to."
        }
      },
      "required": [
        "id",
        "firstName",
        "lastName",
        "email"
      ]
    },
    "Contract": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Contract",
      "type": "object",
      "description": "Represents a contract or service agreement with a client.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Contract entity."
        },
        "clientId": {
          "type": "string",
          "description": "Reference to Client. (Relationship: Client 1:N Contract)"
        },
        "startDate": {
          "type": "string",
          "description": "The start date of the contract.",
          "format": "date-time"
        },
        "endDate": {
          "type": "string",
          "description": "The end date of the contract.",
          "format": "date-time"
        },
        "description": {
          "type": "string",
          "description": "A description of the contract terms."
        },
        "documentUrl": {
          "type": "string",
          "description": "URL to the contract document.",
          "format": "uri"
        }
      },
      "required": [
        "id",
        "clientId",
        "startDate",
        "endDate"
      ]
    },
    "ServiceRequest": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ServiceRequest",
      "type": "object",
      "description": "Represents a service request or ticket submitted by a client.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ServiceRequest entity."
        },
        "clientId": {
          "type": "string",
          "description": "Reference to Client. (Relationship: Client 1:N ServiceRequest)"
        },
        "employeeId": {
          "type": "string",
          "description": "Reference to Employee assigned to the request. (Relationship: Employee 1:N ServiceRequest)"
        },
        "submittedDate": {
          "type": "string",
          "description": "The date the service request was submitted.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "The current status of the service request (e.g., Open, In Progress, Closed)."
        },
        "priority": {
          "type": "string",
          "description": "The priority of the service request (e.g., High, Medium, Low)."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the service request."
        }
      },
      "required": [
        "id",
        "clientId",
        "submittedDate",
        "status",
        "priority",
        "description"
      ]
    },
    "SLA": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SLA",
      "type": "object",
      "description": "Represents a Service Level Agreement.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the SLA entity."
        },
        "clientId": {
          "type": "string",
          "description": "Reference to Client the SLA applies to. (Relationship: Client 1:N SLA)"
        },
        "name": {
          "type": "string",
          "description": "The name or title of the SLA."
        },
        "targetResolutionTime": {
          "type": "string",
          "description": "The target resolution time for service requests covered by this SLA."
        },
        "penaltyClause": {
          "type": "string",
          "description": "Details the penalties if the SLA is not met."
        }
      },
      "required": [
        "id",
        "clientId",
        "name",
        "targetResolutionTime"
      ]
    },
    "OKR": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "OKR",
      "type": "object",
      "description": "Represents an Objective and Key Result.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the OKR entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the OKR."
        },
        "objective": {
          "type": "string",
          "description": "The overarching objective to be achieved."
        },
        "keyResults": {
          "type": "array",
          "description": "The quantifiable key results that measure progress towards the objective.",
          "items": {
            "type": "string"
          }
        },
        "startDate": {
          "type": "string",
          "description": "The start date for the OKR tracking period.",
          "format": "date-time"
        },
        "endDate": {
          "type": "string",
          "description": "The end date for the OKR tracking period.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "objective",
        "keyResults",
        "startDate",
        "endDate"
      ]
    },
    "Document": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Document",
      "type": "object",
      "description": "Represents a document stored in the repository.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Document entity."
        },
        "clientId": {
          "type": "string",
          "description": "Reference to Client the document belongs to. (Relationship: Client 1:N Document)"
        },
        "name": {
          "type": "string",
          "description": "The name of the document."
        },
        "uploadDate": {
          "type": "string",
          "description": "The date the document was uploaded.",
          "format": "date-time"
        },
        "url": {
          "type": "string",
          "description": "The URL where the document is stored.",
          "format": "uri"
        },
        "type": {
          "type": "string",
          "description": "The type or format of the document (e.g., PDF, DOCX)."
        }
      },
      "required": [
        "id",
        "clientId",
        "name",
        "uploadDate",
        "url",
        "type"
      ]
    },
    "Machine": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Machine",
      "type": "object",
      "description": "Represents a machine or equipment in the inventory (NR-12).",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Machine entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the machine or equipment."
        },
        "manufacturer": {
          "type": "string",
          "description": "The manufacturer of the machine."
        },
        "model": {
          "type": "string",
          "description": "The model of the machine."
        },
        "function": {
          "type": "string",
          "description": "The primary function or purpose of the machine."
        },
        "isRiskSource": {
          "type": "boolean",
          "description": "Indicates if the machine is a source of occupational risk."
        },
        "riskDescription": {
          "type": "string",
          "description": "Description of the risks if it is a risk source."
        },
        "maintenanceInfo": {
          "type": "string",
          "description": "Information about maintenance and conservation."
        }
      },
      "required": [
        "id",
        "name",
        "function",
        "isRiskSource",
        "maintenanceInfo"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/clients/{clientId}",
        "definition": {
          "entityName": "Client",
          "schema": {
            "$ref": "#/backend/entities/Client"
          },
          "description": "Stores client company data. The `clientId` parameter is the unique identifier for each client.",
          "params": [
            {
              "name": "clientId",
              "description": "Unique identifier for the client."
            }
          ]
        }
      },
      {
        "path": "/employees/{employeeId}",
        "definition": {
          "entityName": "Employee",
          "schema": {
            "$ref": "#/backend/entities/Employee"
          },
          "description": "Stores employee data. The `employeeId` parameter is the unique identifier for each employee.",
          "params": [
            {
              "name": "employeeId",
              "description": "Unique identifier for the employee."
            }
          ]
        }
      },
      {
        "path": "/clients/{clientId}/contracts/{contractId}",
        "definition": {
          "entityName": "Contract",
          "schema": {
            "$ref": "#/backend/entities/Contract"
          },
          "description": "Stores contracts associated with a client. Includes denormalized `clientId` for authorization independence.",
          "params": [
            {
              "name": "clientId",
              "description": "Unique identifier for the client."
            },
            {
              "name": "contractId",
              "description": "Unique identifier for the contract."
            }
          ]
        }
      },
      {
        "path": "/clients/{clientId}/serviceRequests/{serviceRequestId}",
        "definition": {
          "entityName": "ServiceRequest",
          "schema": {
            "$ref": "#/backend/entities/ServiceRequest"
          },
          "description": "Stores service requests submitted by clients. Includes denormalized `clientId` for authorization independence.",
          "params": [
            {
              "name": "clientId",
              "description": "Unique identifier for the client."
            },
            {
              "name": "serviceRequestId",
              "description": "Unique identifier for the service request."
            }
          ]
        }
      },
      {
        "path": "/clients/{clientId}/slas/{slaId}",
        "definition": {
          "entityName": "SLA",
          "schema": {
            "$ref": "#/backend/entities/SLA"
          },
          "description": "Stores SLAs associated with clients. Includes denormalized `clientId` for authorization independence.",
          "params": [
            {
              "name": "clientId",
              "description": "Unique identifier for the client."
            },
            {
              "name": "slaId",
              "description": "Unique identifier for the SLA."
            }
          ]
        }
      },
      {
        "path": "/okrs/{okrId}",
        "definition": {
          "entityName": "OKR",
          "schema": {
            "$ref": "#/backend/entities/OKR"
          },
          "description": "Stores OKRs (Objectives and Key Results).",
          "params": [
            {
              "name": "okrId",
              "description": "Unique identifier for the OKR."
            }
          ]
        }
      },
      {
        "path": "/clients/{clientId}/documents/{documentId}",
        "definition": {
          "entityName": "Document",
          "schema": {
            "$ref": "#/backend/entities/Document"
          },
          "description": "Stores documents associated with clients. Includes denormalized `clientId` for authorization independence.",
          "params": [
            {
              "name": "clientId",
              "description": "Unique identifier for the client."
            },
            {
              "name": "documentId",
              "description": "Unique identifier for the document."
            }
          ]
        }
      },
      {
        "path": "/clients/{clientId}/machines/{machineId}",
        "definition": {
          "entityName": "Machine",
          "schema": {
            "$ref": "#/backend/entities/Machine"
          },
          "description": "Stores the inventory of machines and equipment for a client, compliant with NR-12. Includes denormalized `clientId` for authorization independence.",
          "params": [
            {
              "name": "clientId",
              "description": "Unique identifier for the client."
            },
            {
              "name": "machineId",
              "description": "Unique identifier for the machine."
            }
          ]
        }
      },
      {
        "path": "/roles_admin/{userId}",
        "definition": {
          "entityName": "role",
          "schema": {
            "$ref": "#/backend/entities/Employee"
          },
          "description": "Documents in this collection represent users with admin privileges. The existence of a document for a given `userId` grants admin access.",
          "params": [
            {
              "name": "userId",
              "description": "Unique identifier for the user."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore data structure is designed to support AptaFlow's core features while adhering to the principles of Authorization Independence, Structural Segregation, Access Modeling, and Data Clarity. The design prioritizes security and scalability by denormalizing authorization data where necessary and structuring collections to enforce consistent security rules.\n\n**Authorization Independence (CRITICAL):**\nAuthorization Independence is achieved through denormalization. For instance, subcollections related to clients (contracts, service requests, documents, SLAs) include the `clientId` field. This allows security rules to validate access based on the authenticated user's relationship to the client without needing to perform `get()` operations to fetch parent document data.\n\n**Structural Segregation:**\nDifferent types of data are stored in separate collections to ensure that all documents within a collection share the same security posture. For example, client data is stored in the `/clients` collection, while employee data is stored in the `/employees` collection.\n\n**Access Modeling:**\n*   **Path-Based Ownership:** The `/users/{userId}` collection is used to store user-specific data. This structure simplifies ownership-based security rules.\n*   **Membership Map (Not Applicable):** Since the application's description doesn't define collaborative data, the membership map is not applicable.\n*   **Global Roles (DBAC):** Global admin roles are stored in the `/roles_admin/{uid}` collection.\n\n**QAPs (Rules are not Filters):**\nThe structure supports secure `list` operations:\n*   Data segregation ensures that listing operations can be scoped to specific collections with consistent security rules.\n*   Denormalization of authorization context (`clientId` in client-related subcollections) allows rules to validate list access based on the current user's relationship to the client, without requiring filtering.\n\n**Invariants:**\n*   Ownership is enforced through the use of `clientId` and `employeeId` fields.\n*   Timestamps (`submittedDate`, `uploadDate`, `startDate`, `endDate`) can be validated in security rules to ensure data integrity.\n*   Denormalized data (e.g., `clientId` in subcollections) allows for maintaining consistency across related documents.\n\nThis structure enables simple, robust, and easily debuggable security rules while supporting the required application features."
  }
}